extends ../layout/index

block contenido
    .py-10
        h1.text-4xl.my10.font-extrabold.text-center Bienes
            span.font-normal Raices
        h2(class="text-gray-900 text-center text-2xl font-extrabold")= pagina

    .bg-white.shadow.py-8.px-4.rounded-lg.max-w-md.mx-auto
        p( class= error ? 'bg-red-500' : 'bg-green-500').py-2.px-5.rounded-lg.w-full.font-bold.text-center.text-white.uppercase= mensaje
        if !error
            a(href="/auth/login" class="text-center font-bold text-md text-slate-600 text uppercase mt-10 block") Ya Puedes Inicia Sesion 
